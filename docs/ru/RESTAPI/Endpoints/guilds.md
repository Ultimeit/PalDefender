# GET /guilds



**Конечная точка:** `GET /v1/pdapi/guilds`

**Аутентификация:** Токен носителя

**Разрешение:** `REST.Guilds.Read`

## Цель

Перечисляет известные гильдии со сводной информацией. Используйте эту конечную точку, чтобы узнать идентификаторы гильдий перед запросом конкретной гильдии.

## Параметры пути

Нет.

## Параметры запроса

Нет.

## Тело запроса

Нет тела запроса.

## Схема ответа

--8<-- "_snippets/restapi/schemas/guilds.md"

## Реакции на ошибки

Тела ошибок используют следующую форму:

```json
{
    "Error": {
        "Code": "ERROR_CODE",
        "Message": "Human-readable message",
        "Details": {}
    }
}
```

| HTTP | Код ошибки | Когда это произойдет |
|------|------------|-----------------|
| `401` | `INVALID_TOKEN` | Заголовок `Authorization` отсутствует, имеет неправильный формат или не соответствует настроенному токену носителя. |
| `403` | `MISSING_PERMISSION` | Токен действителен, но не включает это разрешение конечной точки. |
| `400` | `INVALID_JSON` | Текст запроса был предоставлен, но его не удалось проанализировать как JSON. |
| `400` | `REQUEST_FAILED` | Обратный вызов игрового потока вызвал исключение, или произошел сбой общего преобразователя player/resource. |
| `500` | `REQUEST_TIMEOUT` | Обратный вызов внутреннего игрового потока не завершился в течение 5 секунд. |

## Примеры

### Список всех гильдий

```http
GET /v1/pdapi/guilds
```

### Обновить данные панели управления гильдии.

```http
GET /v1/pdapi/guilds
```

## Сценарии

- Создайте селектор гильдий в админ-панели.
- Найдите `guild_id` для [GET /guild](guild.md).
- Краткий обзор количества баз, количества участников и владения гильдией.
