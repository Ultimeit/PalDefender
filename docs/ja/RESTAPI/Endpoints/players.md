# GET /players



**エンドポイント:** `GET /v1/pdapi/players`

**認証:** ベアラートークン

**許可:** `REST.Players.Read`

## 目的

既知のプレーヤーを識別情報とステータス情報とともにリストします。これを使用して、管理ツール用のプレーヤー セレクターを構築します。

## パスパラメータ

なし。

## クエリパラメータ

なし。

## リクエストボディ

リクエスト本文がありません。

## 応答スキーマ

--8<-- "_snippets/restapi/schemas/players.md"

## エラー応答

エラー本体は次の形状を使用します。

```json
{
    "Error": {
        "Code": "ERROR_CODE",
        "Message": "Human-readable message",
        "Details": {}
    }
}
```

| HTTP |エラーコード |それが起こったとき |
|------|------------|-----------------|
| `401` | `INVALID_TOKEN` | `Authorization` ヘッダーが欠落しているか、形式が不正であるか、構成されたベアラー トークンと一致しません。 |
| `403` | `MISSING_PERMISSION` |トークンは有効ですが、このエンドポイント権限が含まれていません。 |
| `400` | `INVALID_JSON` |リクエスト本文が指定されましたが、JSON として解析できませんでした。 |
| `400` | `REQUEST_FAILED` |ゲーム スレッド コールバックが例外をスローしたか、共有プレーヤー/リソース リゾルバーが失敗しました。 |
| `500` | `REQUEST_TIMEOUT` |内部ゲーム スレッド コールバックは 5 秒以内に完了しませんでした。 |
| `500` | `PLAYER_MANAGER_UNAVAILABLE` |サーバーは Palworld プレーヤー マネージャーにアクセスできませんでした。 |

## 例

### 既知のプレーヤーをすべてリストする

```http
GET /v1/pdapi/players
```

### 管理プレーヤーセレクターを更新する

```http
GET /v1/pdapi/players
```

## シナリオ

- オンライン プレーヤーと既知のプレーヤーのドロップダウンを作成します。
- 報酬、罰、またはインベントリのエンドポイントを呼び出す前に、正しい `UserId` または `PlayerUID` を見つけます。
- メッセージや定期メンテナンスの警告を送信する前に、誰がオンラインであるかを監査します。

## 関連

- [GET /player](player.md) 1 人のプレイヤー用。
- [POST /kick](kick.md)、[POST /ban](ban.md)、および報酬エンドポイントは同じプレイヤー識別子スタイルを使用します。
