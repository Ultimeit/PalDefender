### 200 response schema

| Field | Type | Description |
| --- | --- | --- |
| `Summoned` | object | Details of the spawned NPC. |

`Summoned` contains `Type` (`"NPC"`), `NPCID`, `Level`, `Uncapturable`, `DisableAI`, and the requested `X`, `Y`, and `Z`.
