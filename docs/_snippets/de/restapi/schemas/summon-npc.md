### Schema der 200-Antwort

| Feld | Typ | Beschreibung |
|---|---|---|
| `Summoned` | object | Details des erzeugten NPCs. |

`Summoned` enthält `Type` (`"NPC"`), `NPCID`, `Level`, `Uncapturable`, `DisableAI` sowie die angeforderten Werte `X`, `Y` und `Z`.
